---
title: Quick Start
description: Get started in 5 minutes
---

import { Steps, Callout } from 'nextra/components'

# Quick Start

Get up and running in 5 minutes.

<Steps>
### Install the package

```bash
npm install your-package
```

### Create your first project

```bash
npx your-cli init my-project
cd my-project
```

### Start development

```bash
npm run dev
```
</Steps>

<Callout type="success">
  🎉 You're all set! Visit `http://localhost:3000` to see your project.
</Callout>

## Example Code

Here's an example using our custom CodeBlock component with copy-to-clipboard functionality:

<CodeBlock
  language="typescript"
  code={`// Example TypeScript code
import { useState } from 'react'

function Counter() {
  const [count, setCount] = useState(0)

  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={() => setCount(count + 1)}>
        Increment
      </button>
    </div>
  )
}

export default Counter`}
/>

<CodeBlock
  language="php"
  code={`<?php
// Example PHP code
namespace App\\Http\\Controllers;

use Illuminate\\Http\\Request;

class ApiController extends Controller
{
    public function index(Request $request)
    {
        return response()->json([
            'status' => 'success',
            'data' => ['message' => 'Hello World']
        ]);
    }
}`}
/>

## Next Steps

- [Configuration Options](/getting-started/configuration)
- [Detailed Guides](/guides)
- [API Reference](/api)
